<Page loaded="exports.onPageLoaded">
  <GridLayout rows="*, *">
    <GridLayout row="0" cols="*">
      <chart id="chart" /> 
    </GridLayout>
    <ListView row="1" items="{{ recentLessons.items }}" />
  </GridLayout>
</Page>